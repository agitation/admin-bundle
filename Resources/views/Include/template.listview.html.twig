<template>
    <div class="listview-header">
        <h2>{{ t("Overview") }}</h2>

        <div class="actions">
            <div class="action create">
                <a class="btn btn-primary" href="#!/edit/form/new">{{ t("create new") }}</a>
            </div>

            <form class="action goto form-inline">
                <div class="input-group">
                    <input class="form-control" type="text" name="id" value="" placeholder="{{ t("ID") }}">
                    <span class="input-group-btn">
                        <button class="btn btn-default">{{ t("edit") }}</button>
                    </span>
                </div>
            </form>
        </div>
    </div>

    <form class="listview-search">
        <table>
            <tr>
                <td class="caption">{{ t("Search") }}</td>

                <td class="actions main expand">
                    <button type="submit" class="btn btn-default btn-sm">{{ t("Update list") }}</button>
                    <a>{{ t("Reset parameters") }}</a>
                </td>
            </tr>
        </table>
    </form>

    <div class="listview-table">
        <div class="extra">
            <div class="exporter"></div>
        </div>

        <table>
            <thead><tr></tr></thead>
            <tbody>
                <tr>
                    <td></td>
                    <td class="actions"></td>
                </tr>
                <tr class="noresults">
                    <td>{{ t("This list is currently empty. Please modify your search or create a new object.") }}</td>
                </tr>
            </tbody>
            <tfoot><tr></tr></tfoot>
        </table>
    </div>
</template>
